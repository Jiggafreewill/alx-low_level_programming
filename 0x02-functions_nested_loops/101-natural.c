#include <studio.h>

/**
* main - prints the sum of all multiples of 3 or 5 up to 1024
* Return: Always (Success)
*/
aunt main(void)
{
int i, z = 0;
while (I < 1024)
{
if ((i % 3 == 0) || (I % 5 == 0))
{
z += I;
}
I++;
}
printf(“%d\n” , z);
return (0);
}
